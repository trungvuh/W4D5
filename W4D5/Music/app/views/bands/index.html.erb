<h1> Bands from the Future </h1>

<ul>
  <% @bands.each do |band| %>
    <li><a href=" <%=band_url(band)%> "</a><%= band.name %></li>
  <% end %>
</ul>

<a href=" <%= new_band_url %> ">Create new band</a>
